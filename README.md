# GritSkool

A modern EdTech and IT-industry platform landing page.

## Run the frontend

```powershell
cd frontend
npm install
npm run dev
```

## Run the backend foundation

```powershell
cd backend
npm install
npm run dev
```

The API exposes `GET /api/health` and is ready to grow into student registration, programs, partner, contact, and authentication services.
