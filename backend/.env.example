PORT=4000

# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/gritskool

# SMTP settings used by nodemailer to send waitlist confirmation emails
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
MAIL_FROM="GritSkool <no-reply@gritskool.com>"
